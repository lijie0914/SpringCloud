server:
  ## 端口号不同
  port: 8762
spring:
  application:
    name: eureka-server
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      ## Eureka地址不同
      defaultZone: http://localhost:8761/eureka/